---
/** Sport programmes in action — statement type. Carries a status: this is a
    target for the first deployment phase, not an operating programme. */
import Band from '@/components/Band.astro';
import { getContent, type Locale } from '@/lib/i18n';
import type { Surface, SectionType } from '@/config/site';

interface Props { locale: Locale; type: SectionType; surface: Surface }
const { locale, type, surface } = Astro.props;
const s = getContent(locale).sections.programmes;
---

<Band locale={locale} anchor={s.anchor} title={s.title} titleLines={s.titleLines} type={type} surface={surface} lead={s.lead} status={s.status}>
  <div class="prose-body">
    {s.paragraphs.map((text) => <p>{text}</p>)}
  </div>
</Band>
